# Release Readiness 真机批次 — 人工操作清单（一次完成，约 15 分钟）

前提：手机连着 adb（serial 6036d5b），你已切换正式版 Appause 无障碍为开（已确认）。
自动部分（无需你在场）：R-A 安装 RC 96 覆盖升级 + 权限核验；R-B 充电态延迟基线；R-E 环境恢复核验。

## 你只需要做 4 件事

1. **R-C 静置**：拔掉充电线，锁屏放桌上 **10 分钟**（期间不用手机、不锁模拟器相关操作）。回来解锁手机，告诉助手"回来了"。
2. **R-D-1 真实 Home 手势**（F-27 正式版终检，关键项）：
   - 助手会打开小红书，暂停卡片出现后，你用**手指 Home 手势回桌面一次**——看卡片是否立刻消失、桌面是否干净；
   - B站重复一次。
   - 回答：一次手势够不够？卡片有没有残留？
3. **R-D-2 多任务**：卡片出现时上滑停留进多任务，再点回小红书——回答卡片是否还在/是否正常。
4. **R-D-3 主观体验 + 引导核对**：
   - 卡片出现时有没有明显卡顿/闪烁？（无/轻微/明显）
   - 打开正式版 Appause 首页：顶部"完成设置"检查卡里**电池项**是否出现/是否全绿？截图或口述即可。

## 判定口径（脚本侧）
- R-B/R-C 后延迟：launch→2032 窗口出现 ≤5s = PASS；>60s = F-26 Release 环境风险升级
- R-C 后 release 进程 oom_score_adj < 200 = 用户当前策略足够；= 200 = 需要引导（对照 R-D-3 检查卡状态）
- 全程不点 Continue/Cancel，不触碰用户分组数据；结束后桌面零残留窗口
